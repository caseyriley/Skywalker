import React from 'react';
import fullScreenButton from '../images/fullScreenButton.png';

const FullScreenButton = () => {
  return (
    <>
      <img id={"fullScreenButton"} src={fullScreenButton} alt={""} />
    </>
  )
}
export default FullScreenButton;